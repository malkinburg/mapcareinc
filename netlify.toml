[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Next.js 16 requires a modern Node runtime.
  NODE_VERSION = "20"

# Official Netlify adapter for Next.js — handles SSR and the /api routes.
[[plugins]]
  package = "@netlify/plugin-nextjs"
